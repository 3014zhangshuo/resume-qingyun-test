<div class="row">
      <div class="col-md-8 col-md-offset-2">
          <%= simple_form_for @user do |f| %>

                  <div class="group">
                     <%= f.input :confirm_code %>
                  </div>
                 <div style="margin-top:10px">
                    <%= f.submit "提交", class: "btn btn-default outline", data: { disable_with: "提交中..." } %>
                 </div>
          <% end %>
      </div>
   </div>
