<%= simple_form_for(@order, url: pay_submit_order_path(@order), method: :post) do |f|  %>
<%= f.input :paid_code %>
<%= f.submit "提交", class: "btn btn-default", data: { disable_with: "提交中..." } %>
<% end %>
