<div class="row" style="margin-top:6%;margin-bottom:200px;">
    <div class="col-md-5 col-xs-12" style="text-align:center">
        <div>
            <%= image_tag("wechat399.jpg", style: "max-width:300px;", alt: "微信支付二维码") %>
        </div>
        <div style="padding-bottom:10%;">
            <i style="font-style:italic;">支付后请发截图到微信号"dyzpublic"进行确认
                <br>
                我知道你在想什么，但公司还在注册只能先这样了。。</i>
        </div>
    </div>
    <div class="row" style="margin-top:1.7%;margin-bottom:200px;">
        <div class="col-md-4 col-xs-12">
            <h3 style="text-align:center;">订单详情</h3>
            <table class="table table-hover">

                <tr>
                    <th>订单号:</th>
                    <td><%= @order.token %></td>
                </tr>

                <tr>
                    <th scope="row">订单状态:</th>
                    <% if @order.aasm_state == "order_placed" %>
                    <td>待付款</td>
                    <% end %>
                    <th>创建日期:</th>
                    <td><%= @order.created_at.strftime("%m月%d日%H时%M分") %></td>
                </tr>

                <tr>
                    <th>套餐:</th>
                    <td><%= @order.plan %></td>
                </tr>

                <tr>
                    <th>套餐数量:</th>
                    <td><%= @order.plan_amount %></td>

                    <th>金额:</th>
                    <td><%= @order.money %></td>
                </tr>

                <tr>
                    <th>支付方式:</th>
                    <td><i class="fa fa-weixin" aria-hidden="true"></i>&nbsp;<%= @order.paymethod %></td>

                    <th>更新时间:</th>
                    <td><%= @order.updated_at.strftime("%m月%d日%H时%M分") %></td>
                </tr>

            </table>

        </div>

        <div class="col-md-2 col-xs-12 col-md-offset-1" style="text-align:center">
            <h3 style="text-align:center;">或输入邮件中收到优惠码</h3>
            <%= simple_form_for(@order, url: pay_submit_order_path(@order.token), method: :post) do |f|  %>
            <%= f.input :paid_code, label: false %>
            <%= f.submit "提交", class: "btn btn-primary", data: { disable_with: "提交中..." } %>
            <% end %>
        </div>
    </div>
</div>
