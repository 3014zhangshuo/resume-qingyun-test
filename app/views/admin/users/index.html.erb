<div class="container">
  <div class="col-md-10 col-md-offset-1">
    <table class="table-boldered">
      <thead>
        <tr>
            Email
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr><td><%= user.email %><td><tr>
                  <%# binding.pry %>
                  <% if user.resumes.present? %>
                  <%= user.resumes.last.answer00 %>
                  <% end%>
        <% end %>
      </tbody>
    </table>

  </div>

</div>
